<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>ELMAH LogViewer - @(ViewBag.Title)</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css" rel="stylesheet" />
    <link href="@(Url.Action("_styles", "Shared", new { v = Html.ScriptsVersion() }))" rel="stylesheet" />
    <script src="//code.jquery.com/jquery-2.1.4.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
    <script src="@(Url.Action("_scripts", "Shared", new { v = Html.ScriptsVersion() }))"></script>
@RenderSection("head", false)
</head>
<body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <span class="navbar-brand">
                    <a href="~/"><span class="glyphicon glyphicon-home"></span></a>&nbsp; ELMAH Dashboard
                </span>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="@((this.ViewContext.Controller.GetType().Name == "HomeController") ? "active" : null)"><a href="@Url.Action("Index", "Home")"><span class="glyphicon glyphicon-dashboard"></span> Dashboard</a></li>
                    <li class="@((this.ViewContext.Controller.GetType().Name == "LogsController") ? "active" : null)"><a href="@Url.Action("Index", "Logs")"><span class="glyphicon glyphicon-menu-hamburger"></span> Logs</a></li>
                </ul>
                <form class="navbar-form navbar-right hidden-xs" role="search">
                    <span class="loader"></span>
                </form>
                @*<ul class="nav navbar-nav navbar-right">
                    <li><a href="/MvcElmahDashboard_Readme.html"><span class="glyphicon glyphicon-question-sign"></span> Help</a></li>
                </ul>*@
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>

    <div class="container-fluid body-content">
        @RenderBody()
    </div>

    <div class="container-fluid">
        <hr />
        <footer>
            <p>
                &copy; @(DateTime.Now.Year) Arebis
                | <a href="http://www.arebis.be/MvcElmahDashboard" target="_blank">MvcElmahDashboard</a>
                | Culture: @(System.Threading.Thread.CurrentThread.CurrentCulture.Name)
                <span id="clock-text" class="pull-right"></span>
            </p>
        </footer>
    </div>

    <a id="back-top" href="#top">
        <i class="glyphicon glyphicon-chevron-up"></i>
    </a>

</body>
</html>
